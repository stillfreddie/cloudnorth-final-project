apiVersion: v1
kind: Service
metadata:
  name: cloudnorth-prometheus
  namespace: monitoring  # <-- ✅ Ensure it's in the correct namespace
spec:
  selector:
    app: cloudnorth-prometheus
  type: NodePort
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 31930  # <-- Fixed port for browser access (must be open in EC2 SG)