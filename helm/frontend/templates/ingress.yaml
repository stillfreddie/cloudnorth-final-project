apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloudnorth-frontend-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: frontend.cloudnorth.example.com  # <-- EDIT with real domain
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cloudnorth-frontend-svc
                port:
                  number: 80